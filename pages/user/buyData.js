import React from "react";
import Layout from "../../components/user/Layout";
import withAuth from "../../hocs/withAuth";

const buyData = () => {
  return <Layout>buyData</Layout>;
};

export default withAuth(buyData);
